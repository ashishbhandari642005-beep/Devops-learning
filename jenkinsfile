pipeline {
    agent any

    stages {

        stage('Build Docker Image') {
            steps {
                sh 'docker build -t mannul-project .'
            }
        }

        stage('Run Container') {
            steps {
                sh 'docker run -d -p 8081:3000 mannul-project || true'
            }
        }

    }
}
